{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && npm run build:shared && npm run build --workspace=web",
  "outputDirectory": "dist",
  "installCommand": "cd .. && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
